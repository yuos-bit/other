#!/bin/sh

uci -q batch <<-EOT
	delete firewall.wifiopen
	set firewall.wifiopen=include
	set firewall.wifiopen.type=restore
	set firewall.wifiopen.path=/usr/share/wifiopen/firewall.include
	set firewall.wifiopen.reload=1
	commit firewall
EOT

exit 0
